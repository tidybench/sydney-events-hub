# Sydney Events Hub

Public calendar and map assets for Sydney This Week.

This repository is a generated public output target for the shared `thingstodoinyourcity` runtime. Do not store private scraper state, secrets, or source data here.

Published asset paths:
- `calendars/` for subscribable `.ics` calendars
- `map/` for public `.kml` map files
